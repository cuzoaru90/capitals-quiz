<div class = "container">
  <%= form_for(state) do |f| %>      

    <% if state.errors.any? %>
      <div id="error_explanation">
        <h2>
          <%= pluralize(state.errors.count, "error") %> prohibited
          this guess from being saved:
        </h2>
        <ul>
          <% state.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

      <div class = "shift-right small-text">
        <%= f.label state.name %>
        <%= f.text_field :guess, :style => 'text-align: center width: 30%;'  %>
      </div>

  <% end %>

</div>

