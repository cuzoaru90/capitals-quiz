<%= form_for(state) do |f| %>      

 <!--
    <% if state.errors.any? %>
      <div id="error_explanation">
        <h2>
          <%= pluralize(state.errors.count, "error") %> prohibited
          this guess from being saved:
        </h2>
        <ul>
          <% state.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  

    <div class = "container">
      
    <div class = "jumbotron question">
      <%= f.label state.name %> <br>
      <%= f.text_field :guess, :style => 'text-align: center width: 30%;'  %>

       <div class="form-group">
         <br>
         <%= f.submit "Save answer", class: 'btn btn-success' %>
       </div>

    </div>
 
    <div class = " arrow-container">
      <p class = "arrow-loc"><%= link_to " ", state.prev, class: "glyphicon glyphicon-arrow-left" %>
      <%= link_to " ", state.next, class: "glyphicon glyphicon-arrow-right shift-right" %>
      </p>
    </div>
    
    <%= link_to "Start quiz", root_path, class: "btn btn-default submit-loc", id: "start" %>
    <%= link_to "End quiz", states_answered_path, class: "btn btn-default submit-loc" %>

  </div>

  -->

<% end %>